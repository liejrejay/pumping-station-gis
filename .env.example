# 環境變數範本：複製為 .env（不會被 commit）後填入自己的 key
#
#   cp .env.example .env
#   # 編輯 .env，把下面字串換成你的 Google Maps API key
#
# 申請：https://console.cloud.google.com/apis/credentials
#   需啟用 Maps JavaScript API、綁定 billing，並設定 HTTP referrer 限制
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE
